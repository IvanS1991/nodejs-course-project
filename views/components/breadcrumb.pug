mixin breadCrumb(list)
  ol.breadcrumb
    each location, index in list
      if index === list.length - 1
        li.breadcrumb-item.active #{location}
      else
        li.breadcrumb-item #{location}